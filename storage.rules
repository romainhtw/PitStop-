rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // PDF uploads go through the server-side parse-invoice API route,
    // not directly from the browser. Block all direct client access.
    match /{allPaths=**} {
      allow read: if false;
      allow write: if false;
    }
  }
}
